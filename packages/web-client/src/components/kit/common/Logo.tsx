import React from 'react'
import { Typography } from 'antd'

// style={{ color: '#00ffd0' }}
export default function Logo({ level }: any) {
  return <Typography.Title level={level}>Jekitrack</Typography.Title>
}
